{
  "tasks": {
    "dev": "deno run --allow-net --allow-env --env-file=.env server.ts",
    "build": "deno check server.ts"
  },
  "deploy": {
    "project": "meapi-balance-viewer",
    "entrypoint": "server.ts"
  }
}
